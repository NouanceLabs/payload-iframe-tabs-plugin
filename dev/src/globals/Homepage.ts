import { GlobalConfig } from 'payload/types'

const Homepage: GlobalConfig = {
  slug: 'Homepage',
  fields: [
    {
      name: 'someField',
      type: 'text',
    },
    {
      name: 'source',
      type: 'text',
    },
  ],
}

export default Homepage
